name: Experimental

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform: [linux, windows, darwin]
        version: ["1.1", "2.1", "3.1"]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Test run
        run: |
          echo "running test for ${{ matrix.platform }} ${{ matrix.versoin }}"
